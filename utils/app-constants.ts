/* Error codes to avoid typos */
export const UNAUTHORIZED = 'UNAUTHORIZED';
export const NOT_FOUND = 'NOT_FOUND';

/* For parent id if empty because of our schema design, we chose it to be string null if no parent id, reason in schema design */
export const EMPTY = 'null';

export const MAX_FAVORITE_PAGES_SIDEBAR = 10;
